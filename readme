Create a folder called fintech
Inside this folder create app.py
Copy all your code in this file
create requirements.txt
then launch new terminal

cd fintech_app # This is folder name
create virtual environment: python3.10 -m venv .env
ls -al
source .env/bin/activate
pip install -r requirements.txt
streamlit run app.py --server.address=0.0.0.0 --server.port=8080 --server.enableCORS=false


gcloud run deploy fintech  --source .

